{
  "framework": "vite",
  "installCommand": "if [ -f package.json ]; then npm ci; elif [ -f frontend/package.json ]; then cd frontend; npm ci; else echo 'No package.json found'; exit 1; fi",
  "buildCommand": "if [ -d frontend ]; then cd frontend; npm run build; cd ..; rm -rf dist; cp -r frontend/dist dist; else npm run build; fi",
  "outputDirectory": "dist"
}
